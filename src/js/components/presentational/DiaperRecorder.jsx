import React from "react";
import PropTypes from "prop-types";

const DiaperRecorder = ({ reportDiaper }) => <div>Hello</div>;

DiaperRecorder.propTypes = {
  reportDiaper: PropTypes.func.isRequired
};

export default DiaperRecorder;
